# Talking Buttons — Project Rules

## Before every push

- **Bump the version** in `package.json` and `src/App.tsx` (`APP_VERSION` constant) before committing. Match the semantic version to the scope of changes (major.minor.patch).
